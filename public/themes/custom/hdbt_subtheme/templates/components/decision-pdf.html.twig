{#

#}
{% if decision_pdf is empty %}
  {% set decision_pdf = '#' %}
  {% set hide_decision_pdf = TRUE %}
{% endif %}
<div class="issue__pdf" {% if hide_decision_pdf %}style="display:none;"{% endif %}>
  <a href="{{ decision_pdf }}" data-hds-component="button" data-hds-icon-start="download">
    <span class="hds-button__label">{{ 'Download decision (pdf)'|t }}</span>
  </a>
</div>

