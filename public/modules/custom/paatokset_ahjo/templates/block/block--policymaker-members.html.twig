{%
  set classes = [
  'policymaker-members',
  view_mode ? 'policymaker-members--view-mode-' ~ view_mode|clean_class,
]
%}

{% if meetings %}
<div {{ attributes.addClass(classes) }}>
  <div id="policymaker-members-vue"></div>
</div>
