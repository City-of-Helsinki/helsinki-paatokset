<?php

/**
 * Implements hook_preprocess_block().
 */
function paatokset_decisions_submenu_preprocess_block(&$variables) {
  if($variables['plugin_id'] === 'decisions_submenu') {
    $variables['title'] = $variables['elements']['#attributes']['#title'];
    $variables['tree'] = $variables['elements']['#attributes']['#tree'];
  }
}
